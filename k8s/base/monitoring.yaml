apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: vllm-monitoring
spec:
  selector:
    matchLabels:
      app: llm-adaptive-router
  endpoints:
  - port: 8002
    path: /metrics
    interval: 30s
