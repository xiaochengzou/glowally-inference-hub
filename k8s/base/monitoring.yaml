apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: vllm-monitoring
spec:
  selector:
    matchLabels:
      app: vllm-server
  endpoints:
  - port: 8000
    path: /metrics
    interval: 30s
