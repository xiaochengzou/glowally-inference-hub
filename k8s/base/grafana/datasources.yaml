apiVersion: 1
datasources:
  - name: Google Cloud Monitoring
    type: cloud-monitoring   # Built-in plugin in Grafana 10+, no installation needed
    access: proxy
    isDefault: true
    jsonData:
      authenticationType: gce
      defaultProject: glowally-vllm